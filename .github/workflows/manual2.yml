name: Manual workflow

on:
  push:
    branches:
      - master
      
jobs:
  Automation:
    runs-on: ubuntu-latest
    
    steps:
    - name: Initializacion
      run: echo "Inicializacion de la Ejecucion"
